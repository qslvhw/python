class Solution:
    def searchInsert(self, nums, target):
        left, right = 0, len(nums)
        while i < h:
            i = (left + right) / 2
            if nums[i] < target: left = i + 1
            elif nums[i] > target: right = i - 1
            else: return i
        return left
