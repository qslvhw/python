class Solution:
    def searchInsert(self, nums, target):
        i, h = 0, len(nums)
        while i < h:
            if nums[i] < target:
                return i+1
            else:
                return i
